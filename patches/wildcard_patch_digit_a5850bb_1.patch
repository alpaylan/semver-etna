diff --git a/src/parse.rs b/src/parse.rs
index 12e8840..a541bee 100644
--- a/src/parse.rs
+++ b/src/parse.rs
@@ -291,12 +291,10 @@ fn comparator(input: &str) -> Result<(Comparator, Position, &str), Error> {
 
     let mut pos = Position::Major;
     let (major, text) = numeric_identifier(text, pos)?;
-    let mut has_wildcard = false;
 
     let (minor, text) = if let Some(text) = text.strip_prefix('.') {
         pos = Position::Minor;
         if let Some((_, text)) = wildcard(text) {
-            has_wildcard = true;
             if default_op {
                 op = Op::Wildcard;
             }
@@ -316,7 +314,7 @@ fn comparator(input: &str) -> Result<(Comparator, Position, &str), Error> {
                 op = Op::Wildcard;
             }
             (None, text)
-        } else if has_wildcard {
+        } else if op == Op::Wildcard {
             return Err(Error::new(ErrorKind::UnexpectedAfterWildcard));
         } else {
             let (patch, text) = numeric_identifier(text, pos)?;
